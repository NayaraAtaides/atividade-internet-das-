1. Imprimir "Hello, World!": Crie um programa que exibe a mensagem "Hello,
World!".

#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}

..............................................................................

2. Somar Dois Números: Leia dois números e exiba a soma deles.

#include <stdio.h>

int main() {
    int num1, num2, soma;

    printf("Digite o primeiro número: ");
    scanf("%d", &num1);

    printf("Digite o segundo número: ");
    scanf("%d", &num2);

    soma = num1 + num2;

    printf("A soma é: %d\n", soma);
    return 0;
}

..............................................................................

3. Par ou Ímpar: Leia um número e informe se ele é par ou ímpar.

#include <stdio.h>

int main() {
    int num;

    printf("Digite um número: ");
    scanf("%d", &num);

    if (num % 2 == 0) {
        printf("O número é par.\n");
    } else {
        printf("O número é ímpar.\n");
    }

    return 0;
}

..............................................................................

4. Maior de Dois Números: Leia dois números e exiba o maior deles.

#include <stdio.h>

int main() {
    int num1, num2;

    printf("Digite o primeiro número: ");
    scanf("%d", &num1);

    printf("Digite o segundo número: ");
    scanf("%d", &num2);

    if (num1 > num2) {
        printf("O maior número é: %d\n", num1);
    } else {
        printf("O maior número é: %d\n", num2);
    }

    return 0;
}

..............................................................................

5. Calcular a Média: Leia três números e calcule a média aritmética.

#include <stdio.h>

int main() {
    float num1, num2, num3, media;

    printf("Digite o primeiro número: ");
    scanf("%f", &num1);

    printf("Digite o segundo número: ");
    scanf("%f", &num2);

    printf("Digite o terceiro número: ");
    scanf("%f", &num3);

    media = (num1 + num2 + num3) / 3;

    printf("A média é: %.2f\n", media);
    return 0;
}

..............................................................................

6. Cálculo da Área de um Círculo: Leia o raio de um círculo e calcule sua área.

#include <stdio.h>
#include <math.h>

#define PI 3.14159

int main() {
    float raio, area;

    printf("Digite o raio do círculo: ");
    scanf("%f", &raio);

    area = PI * pow(raio, 2);

    printf("A área do círculo é: %.2f\n", area);
    return 0;
}

..............................................................................

7. Conversão de Temperatura: Converta uma temperatura de Celsius para
Fahrenheit.

#include <stdio.h>

int main() {
    float celsius, fahrenheit;

    printf("Digite a temperatura em Celsius: ");
    scanf("%f", &celsius);

    fahrenheit = (celsius * 9 / 5) + 32;

    printf("A temperatura em Fahrenheit é: %.2f\n", fahrenheit);
    return 0;
}

..............................................................................

8. Tabuada: Exiba a tabuada de um número fornecido.

#include <stdio.h>

int main() {
    int num, i;

    printf("Digite um número para exibir a tabuada: ");
    scanf("%d", &num);

    for (i = 1; i <= 10; i++) {
        printf("%d x %d = %d\n", num, i, num * i);
    }

    return 0;
}

..............................................................................

9. Fatorial de um Número: Calcule o fatorial de um número inteiro positivo.

#include <stdio.h>

int main() {
    int num, fatorial = 1, i;

    printf("Digite um número inteiro positivo: ");
    scanf("%d", &num);

    for (i = 1; i <= num; i++) {
        fatorial *= i;
    }

    printf("O fatorial de %d é: %d\n", num, fatorial);
    return 0;
}

..............................................................................

10. Contagem Regressiva: Exiba uma contagem regressiva de 10 a 1.

#include <stdio.h>


int main() {
    int i;

    for (i = 10; i >= 1; i--) {
        printf("%d\n", i);
    }

    return 0;
}



